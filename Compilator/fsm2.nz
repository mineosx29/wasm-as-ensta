begin lampe : 
    input = active;desactive
    
    state = eteint,allume

    case eteint:
        begin:
            if (active) then
                     next_state=allume
                                    
    end case

    case allume:
    	begin:
        	if (desactive) then
                     next_state=eteint 
            	
    end case

end
